We need to refactor the middleware and update the API endpoint. The kubectl command deploys the struct with a boolean flag. Check the goroutine for linting errors in the interface. Run make lint and go fmt on the package. The function signature takes a context and returns an error. Debug the segfault in the binary. Push the commit and rebase the branch. Undo the last change and delete that line.